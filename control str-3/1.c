#include <stdio.h>
main (){
    int a,b,c;
    a=8;
    b=30;
    c=1;
    (a<b)? (a<c)? printf("A is minimum") :printf("C is minimum") :(b<c)? printf("b is minimum:") : printf("c is minimum") ;

}