#include <stdio.h>
main(){
    int a;
    printf("Enter is a year:");
    scanf(" %d",&a);
    if (a%=4)
    {
        printf("This  is not a leap year.");
    }
    else 
    {
            printf("This is a leap year.");
    }
    
}