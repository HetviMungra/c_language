#include <stdio.h>
main(){
    int a,b;
    printf("Enter the value of a:");
    scanf("%d",&a);
    printf("Enter the value of a:");
    scanf("%d",&b);
    if(a>b)
        {
            printf("\n%d is mix.",a);
        }
        else{
            printf(" %d is mix",b);
            }
        }
    
